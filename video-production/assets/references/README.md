# Reference stills for Pika image-to-video consistency.
# Use the same family photo across all scenes in a film (or per-scene overrides in the manifest).
# Placeholder PNGs are generated by `npm run test:placeholders` for offline testing.
