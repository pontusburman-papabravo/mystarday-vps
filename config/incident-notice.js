/**
 * Temporary service-incident notice (landing banner + info page).
 * Set landingBannerEnabled to false when the banner should be removed.
 */
module.exports = {
  landingBannerEnabled: true,
  infoPagePath: '/viktig-information',
};
