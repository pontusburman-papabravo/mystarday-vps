/**
 * Pictogram library for de sju frågorna — re-exports shared bildstöd library.
 */
const {
  PICTOGRAMS,
  getPictogram,
} = require('./pictogram-library');

module.exports = { PICTOGRAMS, getPictogram };
