# Next Worker Prompt — CAP-007-R1

**Generated by:** Supervisor (CAP-006-R1 handoff)  
**Date:** 2026-07-03  
**Execute this file only. One mission. Then stop.**

---

## Mission ID

`CAP-007-R1`

---

## Mission goal

Prepare IRC-014 (#539) for bundle merge — rebase `cursor/memory-hall-bl012-5e52` onto latest `main`, resolve conflicts, regenerate route inventory if needed, and confirm `test:gate` green.

---

## Branch / PR context

| Field | Value |
|-------|-------|
| **Branch** | `cursor/memory-hall-bl012-5e52` |
| **PR** | #539 (IRC-014) |
| **Base** | `main` |

```bash
git fetch origin
git checkout cursor/memory-hall-bl012-5e52
git rebase origin/main
```

---

## Scope

1. Rebase onto `origin/main`
2. Resolve conflicts (prefer IRC-014 product code + highest SW/cache-version)
3. Regenerate `docs/route-inventory-pre-split.md` if API routes changed vs main
4. `test:gate` green
5. Update handoff files + push (force-with-lease if rebase)

**Out of scope:** merge to main, live deploy, art binaries, family flags

---

## Tests

```bash
export PATH="$HOME/.nvm/versions/node/v20.20.2/bin:$PATH"
sudo pg_ctlcluster 16 main start || true
env -u RESEND_API_KEY -u RESEND_API_KEY_WEEKLY npm run test:gate
```

---

## Stop conditions

- Rebase complete (or documented blocker)
- `test:gate` green
- Handoff files written
- Next Worker prompt exists

---

## Resume command

```
Read .ai/runtime/NEXT_WORKER_PROMPT.md and execute it.
```
