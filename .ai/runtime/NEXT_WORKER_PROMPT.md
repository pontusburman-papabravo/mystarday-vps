# Next Worker Prompt — IRC-014-R1

**Generated by:** Supervisor (CAP-003 handoff)  
**Date:** 2026-07-03  
**Execute this file only. One mission. Then stop.**

---

## Mission ID

`IRC-014-R1`

---

## Mission goal

Rebase IRC-014 (`cursor/memory-hall-bl012-5e52`) onto `cursor/autonomous-relay-resume-b105` and refactor `enterMemoryHall` / `exitMemoryHall` to use CAP-003's `registerWorld('memory_hall', …)` + `enterWorld` / `exitWorld` thin wrappers — **no duplicate portal/chrome animation logic**.

---

## Branch / PR context

| Field | Value |
|-------|-------|
| **Branch** | `cursor/memory-hall-bl012-5e52` (rebase onto relay branch) |
| **PR** | #539 (IRC-014, draft) |
| **Depends on** | CAP-003 merged or present on base (`cursor/autonomous-relay-resume-b105`) |
| **Also update** | #541 if relay branch needs merge commit from this work |

```bash
git fetch origin
git checkout cursor/memory-hall-bl012-5e52
git rebase origin/cursor/autonomous-relay-resume-b105
# resolve conflicts; then implement registry refactor
```

---

## Files to inspect first (read-only)

| File | Why |
|------|-----|
| `public/js/child-living-world-transition.js` | CAP-003 generic API + `registerWorld` |
| `origin/cursor/memory-hall-bl012-5e52:public/js/child-living-world-transition.js` | Current `enterMemoryHall` / `exitMemoryHall` |
| `public/js/child-memory-hall.js` | Mount/deactivate contract |
| `public/js/child-garden.js` | Garden path entry to memory hall |
| `test/living-world-transition.test.js` | Extend for memory_hall registry |

---

## Files likely to edit

| File | Change |
|------|--------|
| `public/js/child-living-world-transition.js` | Register `memory_hall` in `WORLD_REGISTRY`; thin wrappers |
| `test/living-world-transition.test.js` | memory_hall enter/exit smoke via registry |
| `public/sw.js` + `config/cache-version.json` | Bump if JS changes |
| `.ai/knowledge/OPEN_PRS.md` | Note rebase status |
| `.ai/runtime/WORKER_HANDOFF.md` | This Worker's report |
| `.ai/runtime/NEXT_WORKER_PROMPT.md` | Next mission |
| `.ai/runtime/AUTONOMOUS_SESSION.md` | Snapshot |

**Do not edit** (HRC):

- Art binaries / `memory_hall_playable` family allowlist enablement
- Parent `warm_echo` UI

---

## Implementation scope (bounded)

### In scope

1. Rebase IRC-014 onto relay branch (CAP-003 present)
2. `registerWorld('memory_hall', { … })` with hooks from existing `enterMemoryHall` logic:
   - `triggerOpt: 'pathEl'`, `doorWait: false`
   - `beforeMount`: deactivate garden
   - `onMountFail`: remount garden
   - `remountParent` on exit: remount garden, set `_activeWorldId` accordingly
3. `enterMemoryHall` / `exitMemoryHall` → delegate to `enterWorld` / `exitWorld`
4. Tests for memory_hall registry (vm smoke, no art assets)

### Out of scope

- New art assets (HRC-ART-041)
- Live deploy / merge to main
- Enabling `memory_hall_playable` flag for families

---

## Tests to run

```bash
export PATH="$HOME/.nvm/versions/node/v20.20.2/bin:$PATH"
sudo pg_ctlcluster 16 main start || true
env -u RESEND_API_KEY -u RESEND_API_KEY_WEEKLY npm run test:gate
```

---

## Quality gates

| Gate | Required |
|------|----------|
| `test:gate` | ✅ All green |
| Garden + memory_hall transitions | ✅ Unchanged behavior |
| No live deploy | ✅ |

---

## Expected output

1. IRC-014 rebased on CAP-003 generic transition API
2. No duplicated enter/exit animation blocks
3. Tests green
4. Handoff files + next Worker prompt
5. Commit + push to `cursor/memory-hall-bl012-5e52`

---

## Handoff requirements (Worker end)

1. Write `WORKER_HANDOFF.md`
2. Write `NEXT_WORKER_PROMPT.md`
3. Update `AUTONOMOUS_SESSION.md` + `REPOSITORY_STATE.md`
4. Update PR #539 description
5. **Stop.**

---

## Resume command

```
Read .ai/runtime/NEXT_WORKER_PROMPT.md and execute it.
```
