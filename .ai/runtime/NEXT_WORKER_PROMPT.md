# Next Worker Prompt — G9 warm_echo backend complete

**Generated by:** Agent (post G7 + diorama UX)  
**Date:** 2026-07-03  
**Execute this file only. One mission. Then stop.**

---

## Mission ID

`BL-042-backend` — **COMPLETE**

---

## What shipped (PR pending)

| Layer | Files |
|-------|-------|
| Resolver | `src/lib/memory-hall-warm-echo.js` |
| Exhibits merge | `src/lib/memory-hall-exhibit-resolver.js` → `resolveExhibitsForChild` |
| Playable | `src/lib/memory-hall-playable.js` |
| Parent opt-in API | `child_view_config.memory_hall.warm_echo_enabled` via PATCH `/api/children/:id/view-config` |
| Merge helper | `src/lib/child-view-config.js` |
| Tests | `test/memory-hall-warm-echo.test.js` (in test:gate) |

**Default:** `warm_echo_enabled: false` — no frame until parent opts in via API (UI later).

---

## Human gates still open

| ID | What | Agent may not |
|----|------|---------------|
| HRC-ART-041 | Scene WebP | Commit art binaries |
| HRC-PARENT-042 | Parent opt-in **UI + final copy** | Ship parent settings surface |

---

## Suggested next missions (pick one)

1. **CAP-008-R1** — Rebase/close #541 if still diverged from main (relay docs only)
2. **IRC bundle 007–012** — Cherry-pick governance/a11y/test PRs onto main
3. **G8 prep** — Blocked until art approved (user deferred UI/bilder)

---

## Resume command

```
Read .ai/runtime/NEXT_WORKER_PROMPT.md and continue from suggested next missions.
```
