# Next Worker Prompt — CAP-005

**Generated by:** Supervisor (CAP-004-R1 handoff)  
**Date:** 2026-07-03  
**Execute this file only. One mission. Then stop.**

---

## Mission ID

`CAP-005`

---

## Mission goal

Wire `memory-hall-asset-pipeline.js` into the child dashboard load order and `child-memory-hall.js` mount path — mirroring how `garden-asset-pipeline.js` integrates with `child-garden.js`. Reversible prep for Art HRC (BL-041); no binary assets.

---

## Branch / PR context

| Field | Value |
|-------|-------|
| **Branch** | `cursor/autonomous-relay-resume-b105` |
| **PR** | #541 (IRC-016) |
| **Reference** | `public/js/child-garden.js` + `garden-asset-pipeline.js` pattern |

```bash
git fetch origin
git checkout cursor/autonomous-relay-resume-b105
git pull
```

---

## Files to inspect first (read-only)

| File | Why |
|------|-----|
| `public/js/garden-asset-pipeline.js` | Pattern to mirror |
| `public/js/memory-hall-asset-pipeline.js` | Stub already exists |
| `public/js/child-garden.js` | `pipeline()`, `bindAssetWatch`, scene bg |
| `public/js/child-memory-hall.js` | Mount/render — add pipeline hook |
| `public/child-dashboard.html` | Script load order |
| `test/memory-hall-asset-pipeline.test.js` | Existing stub tests |

---

## Files likely to edit

| File | Change |
|------|--------|
| `public/child-dashboard.html` | Add `memory-hall-asset-pipeline.js` script tag |
| `public/js/child-memory-hall.js` | `pipeline()`, scene bg via `scenePictureHtml`, `bindAssetWatch` |
| `public/sw.js` + `config/cache-version.json` | Bump if JS changes |
| `test/memory-hall-playable.test.js` or new contract test | Assert pipeline wired in HTML |

**Do not edit:** art binaries, `memory_hall_playable` allowlist

---

## Implementation scope (bounded)

### In scope

1. Load `memory-hall-asset-pipeline.js` after `scene-asset-pipeline.js` in child-dashboard
2. `child-memory-hall.js`: use `MemoryHallAssetPipeline` for scene background (graceful fallback when assets missing — `preloadWhenNoImage: false` already set)
3. `bindAssetWatch` or equivalent exit-on-fail (mirror garden)
4. Tests + SW bump

### Out of scope

- Committing WebP art files (HRC-ART-041)
- CSS beyond minimal scene-bg class if needed
- Live deploy

---

## Tests to run

```bash
export PATH="$HOME/.nvm/versions/node/v20.20.2/bin:$PATH"
sudo pg_ctlcluster 16 main start || true
env -u RESEND_API_KEY -u RESEND_API_KEY_WEEKLY npm run test:gate
```

---

## Expected output

1. Memory hall mount uses asset pipeline when available; empty-state fallback when not
2. `test:gate` green
3. Handoff files + next Worker prompt
4. Commit + push to `cursor/autonomous-relay-resume-b105`

---

## Stop conditions

- `test:gate` green
- Handoff files written
- Next Worker prompt exists

---

## Resume command

```
Read .ai/runtime/NEXT_WORKER_PROMPT.md and execute it.
```
