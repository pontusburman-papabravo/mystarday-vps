{
  "$schema": "https://www.cursor.com/schemas/environment.schema.json",
  "install": "export PATH=\"$HOME/.nvm/versions/node/v20.20.2/bin:$PATH\" && (test -x \"$HOME/.nvm/versions/node/v20.20.2/bin/node\" || (nvm install 20 && export PATH=\"$HOME/.nvm/versions/node/v20.20.2/bin:$PATH\")) && npm install --legacy-peer-deps && (sudo pg_ctlcluster 16 main start 2>/dev/null || true) && (sudo -u postgres psql -tc \"SELECT 1 FROM pg_roles WHERE rolname='stjarndag'\" 2>/dev/null | grep -q 1 || sudo -u postgres createuser -s stjarndag 2>/dev/null || true) && (sudo -u postgres psql -c \"ALTER USER stjarndag WITH PASSWORD 'stjarndag';\" 2>/dev/null || true) && (sudo -u postgres psql -tc \"SELECT 1 FROM pg_database WHERE datname='stjarndag'\" 2>/dev/null | grep -q 1 || sudo -u postgres createdb -O stjarndag stjarndag 2>/dev/null || true) && DATABASE_URL=\"${DATABASE_URL:-postgresql://stjarndag:stjarndag@localhost:5432/stjarndag}\" npm run migrate 2>/dev/null || true"
}
